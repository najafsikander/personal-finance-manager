const BackButton = () => {
  return (
    <button className="border border-white rounded-md py-2 px-4 bg-transparent text-white hover:bg-white hover:cursor-pointer hover:text-slate-800 transition-colors duration-10">
      BACK
    </button>
  );
};

export default BackButton;
